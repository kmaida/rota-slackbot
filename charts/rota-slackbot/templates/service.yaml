apiVersion: v1
kind: Service
metadata:
  name: {{ include "rota-slackbot.fullname" . }}
  labels:
    {{- include "rota-slackbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rota-slackbot.selectorLabels" . | nindent 4 }}
